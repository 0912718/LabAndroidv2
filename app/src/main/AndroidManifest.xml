<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="ictlab.app1">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        tools:replace="android:icon"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.Fullscreen">
        <activity
            android:name=".Main.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Main.afterLoginSuccess"/>
        <activity android:name=".Booking.pickerDateTime"/>
        <activity android:name=".Booking.chooseBuilding"/>
        <activity android:name=".Booking.chooseRoomH4" />
        <activity android:name=".qrCodeClass" />
        <activity android:name=".settingsClass" />
        <activity android:name=".notificationClass" />
        <activity android:name=".reservationClass" />
        <activity android:name=".helpClass" />
        <activity android:name=".classroomData" />
        <activity android:name=".ListTest.ListTestActivity" />

    </application>

</manifest>
